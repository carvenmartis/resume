export interface IResumeProps {
  header: {
    name: string;
    title: string;
  };
  contact: {
    location: string;
    phone: string;
    email: string;
  };
  profile: {
    heading: string;
    description: string;
  };
}
